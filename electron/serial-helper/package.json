{
  "name": "voting-serial-helper",
  "private": true,
  "version": "1.0.0",
  "description": "Node serial reader spawned by NativePHP main process. Reads Qomo voting transmitter frames over USB-serial and POSTs them to Laravel.",
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js",
    "rebuild": "electron-rebuild -f -w serialport"
  },
  "dependencies": {
    "express": "^4.21.0",
    "serialport": "^13.0.0"
  }
}
