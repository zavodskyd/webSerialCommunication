<div class="min-h-screen bg-slate-100"><div class="mx-auto max-w-4xl space-y-6 px-4 py-8"><a href="{{ route('elections.edit', $voting) }}" wire:navigate class="text-sm font-semibold text-emerald-700">← Späť na voľby</a><section class="rounded-[2rem] bg-white p-6 shadow-sm"><h1 class="text-3xl font-semibold text-slate-900">Doplnenie kandidáta</h1><form wire:submit="createAndOpenAdmission" class="mt-6 grid gap-4 md:grid-cols-2"><input wire:model.blur="firstName" placeholder="Meno" class="rounded-xl border-slate-300 px-3 py-2"><input wire:model.blur="lastName" placeholder="Priezvisko" class="rounded-xl border-slate-300 px-3 py-2"><select wire:model="deviceGroupId" class="rounded-xl border-slate-300 px-3 py-2 md:col-span-2"><option value="">Bez lokality — Kontrolná komisia, všetky zariadenia</option>@foreach($groups as $group)<option value="{{ $group->id }}">{{ $group->name }} — príslušné predstavenstvo</option>@endforeach</select><button class="rounded-xl bg-emerald-600 px-4 py-3 font-semibold text-white md:col-span-2">Otvoriť doplnenie</button></form></section><section class="rounded-[2rem] bg-white p-6 shadow-sm"><h2 class="text-xl font-semibold">Návrhy</h2>@foreach($admissions as $admission)<div class="mt-3 rounded-xl bg-slate-50 px-4 py-3">{{ $admission->first_name }} {{ $admission->last_name }} — {{ $admission->status }}</div>@endforeach</section></div></div>
</div>
