<div class="min-h-screen bg-slate-100 p-6"><div class="mx-auto grid max-w-6xl gap-6 lg:grid-cols-[18rem_1fr]"><aside class="rounded-3xl bg-white p-4 shadow-sm">@foreach($contests as $item)<button wire:click="selectContest({{ $item->id }})" class="mt-2 block w-full rounded-xl px-3 py-2 text-left {{ $contest->id === $item->id ? 'bg-emerald-600 text-white' : 'bg-slate-100' }}">{{ $item->name }}</button>@endforeach</aside><main class="rounded-3xl bg-white p-6 shadow-sm"><h1 class="text-2xl font-semibold">{{ $contest->name }}</h1>@if(!$round)<button wire:click="createRound" class="mt-5 rounded-xl bg-emerald-600 px-4 py-3 font-semibold text-white">Vytvoriť kolo</button>@else<div class="mt-5 flex gap-3"><button wire:click="openRound" class="rounded-xl bg-emerald-600 px-4 py-3 font-semibold text-white">Spustiť</button><button wire:click="closeRound" class="rounded-xl bg-rose-600 px-4 py-3 font-semibold text-white">Zastaviť</button></div><p class="mt-4">Kolo {{ $round->round_number }} · {{ $round->status }} · väčšina {{ $results['majority_threshold'] }}</p>@foreach($results['candidates'] as $candidate)<div class="mt-2 flex justify-between rounded-xl bg-slate-100 px-4 py-3"><span>{{ $candidate['first_name'] }} {{ $candidate['last_name'] }}</span><strong>{{ $candidate['weighted_total'] }}</strong></div>@endforeach@endif</main></div>
</div>
