<article wire:key="contest-{{ $contest['id'] }}" class="rounded-[1.5rem] border border-slate-200 bg-white p-5 shadow-sm">
    <h2 class="text-xl font-semibold text-slate-900">{{ $contest['name'] }}</h2>
    <div class="mt-4 space-y-3">@forelse ($contest['candidates'] as $candidate)<form wire:submit="saveCandidate({{ $candidate['id'] }})" wire:key="candidate-{{ $candidate['id'] }}" class="grid gap-2 sm:grid-cols-[1fr_1fr_auto_auto]"><input type="text" wire:model.blur="contestRows.{{ array_search($contest['id'], array_column($contestRows, 'id')) }}.candidates.{{ array_search($candidate['id'], array_column($contest['candidates'], 'id')) }}.first_name" class="rounded-xl border-slate-300 px-3 py-2"><input type="text" wire:model.blur="contestRows.{{ array_search($contest['id'], array_column($contestRows, 'id')) }}.candidates.{{ array_search($candidate['id'], array_column($contest['candidates'], 'id')) }}.last_name" class="rounded-xl border-slate-300 px-3 py-2"><button class="text-sm font-semibold text-emerald-700">Uložiť</button><button type="button" wire:click="removeCandidate({{ $candidate['id'] }})" class="text-sm font-semibold text-rose-700">Odstrániť</button></form>@empty<p class="text-sm text-slate-500">Kandidátka je prázdna.</p>@endforelse</div>
    <form wire:submit="addCandidate({{ $contest['id'] }})" class="mt-4 grid gap-2 sm:grid-cols-3"><input x-ref="candidate-first-{{ $contest['id'] }}" type="text" wire:model.blur="candidateDrafts.{{ $contest['id'] }}.first_name" placeholder="Meno" class="rounded-xl border-slate-300 px-3 py-2"><input type="text" wire:model.blur="candidateDrafts.{{ $contest['id'] }}.last_name" placeholder="Priezvisko" class="rounded-xl border-slate-300 px-3 py-2"><button class="rounded-xl bg-emerald-600 px-3 py-2 text-sm font-semibold text-white">Pridať</button></form>
</article>
